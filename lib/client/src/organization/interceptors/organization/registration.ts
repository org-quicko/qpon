import type { Interceptors } from "../types";

/*
 * This file is only ever generated once on the first generation and then is free to be modified.
 * Any interceptors you wish to add should be registered in the initInterceptors function. Feel free to define them
 * in this file or in separate files in the interceptors folder.
 */

export function initInterceptors(_interceptors: Interceptors) {
	// Add interceptors by calling:
	//   interceptors.registerBeforeRequestInterceptors([{ beforeRequest: (config) => { ... return config; } }])
	//   interceptors.registerAfterResponseInterceptors([{ afterResponse: (response) => { ... return response; } }])
	//
	// Interceptors are registered per SDK instance, and are valid for the lifetime of the SDK instance
}
